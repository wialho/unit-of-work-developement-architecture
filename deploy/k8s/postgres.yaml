apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: workflow-pipeline
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: workflow
            - name: POSTGRES_USER
              value: workflow
            - name: POSTGRES_PASSWORD
              value: workflow
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: workflow-pipeline
spec:
  selector:
    app: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
